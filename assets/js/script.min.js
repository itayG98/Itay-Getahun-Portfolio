const mail = "itai.g98@gmail.com";
!(function () {
  "use strict";
  var e = document.querySelector(".scroll-to-top");
  e &&
    window.addEventListener("scroll", function () {
      var o = window.pageYOffset;
      e.style.display = o > 100 ? "block" : "none";
    });
  var o = document.querySelector("#mainNav");
  if (o) {
    var n = o.querySelector(".navbar-collapse");
    if (n) {
      var t = new bootstrap.Collapse(n, { toggle: !1 }),
        r = n.querySelectorAll("a");
      for (var a of r)
        a.addEventListener("click", function (e) {
          t.hide();
        });
    }
    var c = function () {
      (void 0 !== window.pageYOffset
        ? window.pageYOffset
        : (
            document.documentElement ||
            document.body.parentNode ||
            document.body
          ).scrollTop) > 100
        ? o.classList.add("navbar-shrink")
        : o.classList.remove("navbar-shrink");
    };
    c(), document.addEventListener("scroll", c);
  }
})();
function SubmitForm(form) {
  window.open(
    "mailto:" +
      `${mail}` +
      "?subject=I%20reched%20you%20From%20Portfolio%20page%20:" +
      form.name.value +
      "%20&body=Hey%20my%20Name%20is:%20" +
      form.name.value +
      "%20" +
      "\n" +
      `${form.message.value}%20` +
      `From ${form.name.value}%20` +
      "Call%20me:%20" +
      `${form.phone.value}` +
      "%20" +
      "\n" +
      `&cc=${form.email.value}`
  );
  return false; /* cancel submit or else page reloads */
}
